import styled from 'styled-components'
import { theme } from '../constants'

export const Divider = styled.div`
    height: 1px;
    padding-bottom: 0px;
    background-color: ${theme.grey.bgLight};
`